# Runtime dependencies — kept in sync with pyproject.toml.
# Preferred install: `pip install -e .` (runtime) or `pip install -e ".[dev]"` (runtime + tests).
pyaudio==0.2.14
PyQt6==6.10.2
faster-whisper==1.2.1
numpy>=1.24.0,<2.0.0
ollama==0.6.2
scipy==1.13.1
